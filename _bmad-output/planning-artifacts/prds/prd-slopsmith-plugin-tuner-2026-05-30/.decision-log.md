# Decision Log — slopsmith-plugin-tuner PRD

_Canonical audit trail. Every decision, change, assumption, and override is recorded here as the conversation unfolds._

---

## 2026-05-30 — Initial draft

**Stakes calibration**: Hobby/community open-source. Product has real user adoption (dozens of players). PRD calibrated at 3–5 pages — above pure solo but below enterprise rigor.

**Working mode**: Fast path selected by OmikronApex.

**Source**: PRD derived primarily from codebase (screen.js, routes.py, utils/tuning-utils.js, visualization/*.js, plugin.json, README.md) plus user-supplied context:
- Plugin exists to fill the tuner gap in Slopsmith (personal itch, now community-adopted).
- Plugin is feature-complete at v1.2.5; future work = bugfixes, refactors, new visualizations/tunings.
- Self-containment (no external JS libraries) is a hard constraint.
- Accuracy and UX stability are the primary quality goals.

**PRD finalized 2026-05-30** — status set to `final`.

**OQ-02 resolved (corrected 2026-05-30)**: Default is the canonical default visualization for new installs (matches backend `config.json`). `screen.js` initialising to `"strobe"` is a code bug — it should be corrected to `"default"`. PRD updated: FR-17 and §8 reflect resolved state.

**OQ-01 deferred**: `ScriptProcessorNode` is deprecated. No action until browser enforcement; tracked as open question.

**Reviewer/reconciliation findings addressed (2026-05-30)**:
- FR-02: clarified note display is class-only (not octave-qualified)
- FR-10: added "Current Song" option lifecycle (player screen + metadata required)
- FR-12: noted 5-string bass label fallback limitation
- FR-17: added ±2 cents in-tune threshold; documented strobe as new-install default
- FR-28/FR-29: mutual exclusivity of buttons made explicit
- FR-32: documented that "Current Song" is not persisted as lastTuning
- FR-34: added degraded-mode behavior for config-fetch failure
- NFR-04: rewritten to be orthogonal constraint (no audio logic on main thread)
- NFR-06: rewritten as visualization module API contract
